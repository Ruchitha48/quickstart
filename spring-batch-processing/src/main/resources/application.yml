server:
  port: 4040
spring:
  datasource:
    password: system
    url: jdbc:mysql://localhost:3306/sys
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  batch:
    jdbc:
      initialize-schema: always